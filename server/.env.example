# SIH Portal - Environment Variables for Local Testing
# Copy this to .env in the server directory and update with your values

# Option 1: MongoDB Atlas (RECOMMENDED for testing)
# Go to https://cloud.mongodb.com and create free M0 cluster
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@cluster0.xxxxx.mongodb.net/
MONGODB_DB=sih_portal_test

# Option 2: Local MongoDB (if you have MongoDB installed)
# MONGODB_URI=mongodb://localhost:27017
# MONGODB_DB=sih_portal_local

# Server Configuration
PORT=10000
NODE_ENV=development

# Instructions for local testing:
# 1. Create MongoDB Atlas account (free)
# 2. Create M0 cluster 
# 3. Create database user
# 4. Get connection string
# 5. Replace USERNAME:PASSWORD above
# 6. Copy this file to .env
# 7. Run: npm run dev
